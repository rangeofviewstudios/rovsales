# {Vertical} — Sales Sheet

> Copy this file to `verticals/{vertical}.md` and fill every {bracket}. Delete these
> quote lines when done. Read `00-SPINE.md` first: it already holds the offers, the
> A/B/C system, the voice rules, objections, and tracking. Only write what's DIFFERENT
> about this target below. Don't repeat the spine.

**Who:** {ICP in one line. Who reinvests, who decides, why it's a fast yes/no.}
**Skip:** {Who to never contact. No local decision-maker = no deal.}

---

## The Offer: {vertical-specific name} ({the three-word promise})

{One paragraph: the target's core truth + where money leaks. Two or three leaks max,
in their words, with a villain named.}

| Engine | What it does | The money it wins/saves |
|---|---|---|
| **{Leak 1 → engine}** | {what we build} | {the leak in their words} |
| **{Leak 2 → engine}** | {what we build} | {the leak in their words} |
| **{Leak 3 → engine}** | {what we build} | {the leak in their words} |

**Proof:** {The one locked number, OR "no case study yet, the built asset IS the proof."
Adjacent proof (Bando) if usable. Never invent a stat.}

**The close (show, don't tell):** {The one asset you build for this vertical that flips
them: mockup / Reel / audit. Why this one is strongest here.}

**Bar test:** *"{The whole pitch out loud in 3-4 sentences: the truth, the leaks, what
we do, the free thing we'll show them.}"*

---

## Which channel to use (by {size/type})

| {Size/type} | Channel |
|---|---|
| {smallest} | **{channel}** — {why} |
| {middle} | **{channel}** |
| {biggest} | **{channel}** |

**Send timing:** {days}, **{the dead-hour window for this vertical}**. Avoid {when
they're heads-down}.

---

## The three angles to A/B/C test

- **A — Diagnosis:** {the leak named up front, in their words}.
- **B — Curiosity:** {the tease that makes them ask}.
- **C — {the built asset}:** {you already made it, want to send it}.

CTA throughline: **"{want me to walk you through it? / send it over?}"**

---

## Instagram DM

**A — Diagnosis:**
```
{message}
```

**B — Curiosity:**
```
{message}
```

**C — {asset}:**
```
{message}
```

---

## Email

**A — Diagnosis.** Subjects: `{subj}` / `{subj}`
```
Hi {First name},

{body}

Suchet
ROV Studios
```

**B — Curiosity.** Subjects: `{subj}` / `{subj}`
```
{body}
```

**C — {asset}.** Subjects: `{subj}` / `{subj}`
```
{body}
```

---

## LinkedIn
> Only fill if this vertical lives on LinkedIn (agents, B2B). Delete if not.
```
{message}
```

---

## Rules of the road (vertical overrides only)
- **Play:** {which angle to lean on for THIS vertical and why}.
- **Vocabulary:** allowed: {their words}. Banned: {vendor words that read as commodity}.
- {Any villain / positioning rule specific to this vertical.}
- {The one thing a rep would get wrong here if the spine didn't warn them.}

*Proof note (Suchet): {what to lock, what to swap in once a real win is documented}.*
